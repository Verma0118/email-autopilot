# secrets and tokens — never commit
mcp-exa.json
*.token
*token*.json

# runtime state and personal data
state/
logs/
queue/
.venv/
__pycache__/
*.pyc

# generated artifacts live outside the repo but belt-and-braces
dashboard.html
digests/
contacts*.json
